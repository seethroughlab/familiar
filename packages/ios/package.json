{
  "name": "@familiar/ios",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:cap": "BUILD_TARGET=capacitor vite build",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios",
    "cap:build": "pnpm --filter @familiar/web run build:cap && npx cap sync ios && npx cap open ios",
    "deploy-device": "./scripts/deploy-device.sh",
    "release-testflight": "./scripts/release-testflight.sh"
  },
  "dependencies": {
    "@familiar/frontend": "workspace:*",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "@types/node": "^24.10.1",
    "@vitejs/plugin-react": "^5.1.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
