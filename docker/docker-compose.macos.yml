# macOS override: replace journald logging (Linux-only) with default json-file driver
# Usage: docker compose -f docker-compose.prod.yml -f docker-compose.macos.yml up -d

services:
  postgres:
    logging:
      driver: json-file
  redis:
    logging:
      driver: json-file
  api:
    logging:
      driver: json-file
